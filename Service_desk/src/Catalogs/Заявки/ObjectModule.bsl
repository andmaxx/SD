Процедура ПередЗаписью(Отказ)
	
	Если НЕ ЗначениеЗаполнено(Ссылка) ИЛИ Статус <> Ссылка.Статус Тогда
		Если Статус = Перечисления.СтатусыЗаявок.ВОжиданииИнформацииЗаявителя Тогда
			ДатаВыполнения = ТекущаяДата();
		ИначеЕсли Статус = Перечисления.СтатусыЗаявок.Закрыта Тогда
			ДатаПодтвержденияВыполения = ТекущаяДата();
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры
